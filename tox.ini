# For more information about tox, see https://tox.readthedocs.io/en/latest/
[tox]
envlist = py{39,310,311,312}-{linux,macos,windows}
isolated_build=true

[gh-actions]
python =
    3.9: py39
    3.10: py310
    3.11: py311
    #3.12: py312

[gh-actions:env]
PLATFORM =
    ubuntu-latest: linux
    macos-latest: macos
    windows-latest: windows

[testenv]
setenv =
    PYTHONPATH = {toxinidir}/src
    # Windows-specific settings to prevent DLL conflicts
    windows: OPENCV_IO_ENABLE_OPENEXR = 0
    windows: OPENCV_IO_ENABLE_JASPER = 0
    windows: KMP_DUPLICATE_LIB_OK = TRUE
    windows: OMP_NUM_THREADS = 1
platform =
    macos: darwin
    linux: linux
    windows: win32
passenv =
    CI
    GITHUB_ACTIONS
    DISPLAY
    XAUTHORITY
    NUMPY_EXPERIMENTAL_ARRAY_FUNCTION
    PYVISTA_OFF_SCREEN
    QT_QPA_PLATFORM
    MPLBACKEND
    PYTHONUNBUFFERED
    PYTHONDONTWRITEBYTECODE
    OPENCV_IO_ENABLE_OPENEXR
    OPENCV_IO_ENABLE_JASPER
    KMP_DUPLICATE_LIB_OK
extras =
    testing
deps =
    pytest
    pytest-cov
    pytest-timeout
    napari
    numpy>=1.23.0,<2.0
    psygnal>=0.8.0
commands =
    pytest --cov=napari_tmidas --cov-report=xml --timeout=300 -v
